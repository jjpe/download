extern crate curl;
extern crate url;

use curl::easy::Easy;
use curl::Error as CurlErr;
use std::env;
use std::fs::{DirEntry, File};
use std::io;
use std::io::{Read, Write};
use std::path::{Path, PathBuf};
use std::process;
use std::str;
pub use url::{Url};













#[cfg(test)]
mod tests {
    #[test]
    fn it_works() {
    }
}
